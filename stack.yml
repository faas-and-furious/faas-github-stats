provider:
  name: faas
  gateway: http://localhost:8080

functions:
  github-stats:
    lang: node
    handler: ./github-stats
    image: kenfdev/github-stats:0.9.0
    environment:
      read_timeout: 30
      API_KEY_NAME: kenfdev
    secrets:
      - github-api-keys
